apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: mysql-pvc
  namespace: portfolio
spec:
  storageClassName: mysql-sc
  resources:
    requests:
      storage: 1Gi
  accessModes:
    - ReadWriteOnce
